start
    float a;
    float b, result;
    float c,x,total,y;

    a = 2.0;
    x = 1;
    scan (b);
    scan (y);
    result = (a*b + 1) / a;
    print ({Resultado: });
    print (result);
    print ({Total: });
    total = y / x;
    print ({Total: });
    print (total);
exit
